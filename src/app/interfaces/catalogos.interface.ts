export interface Ciudad {
    _id?: string;
    nombre: string;
    activo: boolean;
  }
  
  export interface Puesto {
    _id?: string;
    nombre: string;
    descripcion?: string;
    activo: boolean;
  }
  
  export interface Departamento {
    _id?: string;
    nombre: string;
    descripcion?: string;
    activo: boolean;
  }